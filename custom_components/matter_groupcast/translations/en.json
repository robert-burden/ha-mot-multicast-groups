{
  "config": {
    "step": {
      "user": {
        "title": "Matter groupcast light",
        "description": "Pick a Home Assistant light helper group of Matter bulbs. On/Off, brightness, color, and color temperature use Matter IPv6 group multicast when the companion add-on is running and members are provisioned.",
        "data": {
          "source_entity": "Light group",
          "group_name": "Matter group name",
          "group_id": "Matter Group ID"
        }
      }
    },
    "error": {
      "not_a_group": "That entity is not a light group (it has no member entity_id list)."
    },
    "abort": {
      "matter_not_setup": "Set up the official Matter integration first.",
      "already_configured": "This light group is already configured."
    }
  }
}
